import { StringModificationPipe } from './string-modification.pipe';

describe('StringModificationPipe', () => {
  it('create an instance', () => {
    const pipe = new StringModificationPipe();
    expect(pipe).toBeTruthy();
  });
});
